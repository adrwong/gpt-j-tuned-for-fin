{
  "layers": 28,
  "d_model": 4096,
  "n_heads": 16,
  "n_vocab": 50400,
  "norm": "layernorm",
  "pe": "rotary",
  "pe_rotary_dims": 64,

  "seq": 2048,
  "cores_per_replica": 8,
  "per_replica_batch": 1,
  "gradient_accumulation_steps": 16,

  "warmup_steps": 500,
  "anneal_steps": 4500,
  "lr": 1.2e-4,
  "end_lr": 1.2e-5,
  "weight_decay": 0.1,
  "total_steps": 5000,

  "tpu_size": 256,

  "bucket": "bucket_for_tpu_gptj",
  "model_dir": "tuned_to_finbase_2",

  "train_set": "fin.train.index",
  "val_set": {},
  
  "eval_harness_tasks": [

  ],

  "val_batches": 622,
  "val_every": 500,
  "ckpt_every": 500,
  "keep_every": 10000,

  "name": "tuned_to_base",
  "wandb_project": "mesh-transformer-jax",
  "comment": ""
}
